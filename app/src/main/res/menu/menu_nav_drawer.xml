<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:checked="true"
            android:icon="@drawable/ic_menu_home"
            android:title="@string/menu_drawer_home" />
        <item
            android:id="@+id/nav_app_list"
            android:icon="@drawable/ic_menu_cpu"
            android:title="@string/menu_drawer_instrumentation" />

        <item
            android:id="@+id/nav_modules"
            android:icon="@drawable/ic_menu_cpu"
            android:title="@string/menu_drawer_modules" />
    </group>

    <item
        android:id="@+id/nav_settings"
        android:icon="@drawable/ic_menu_manage"
        android:title="@string/action_settings" />
</menu>
